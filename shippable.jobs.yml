jobs:
  - name: public-test_runCI
    type: runCI
    steps:
      - IN: public-test_ciRepo
      - OUT: my_image

  - name: test
    type: runCLI
    on_start:
      - script: echo 'This block executes when the TASK starts'
    steps:
      - IN: public-test_ciRepo
      - TASK:
        - script: shippable_replace file1.json